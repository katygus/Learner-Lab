/*
API service functions - a layer to abstract API calls. 

For example would contain functions like:

getAllStrategies() -> moved from where it is now directly in the component

createStrategy(data)

updateStrategy(id, data)

deleteStrategy(id)
*/


// Example structure
export const fetchStrategies = (filters) => {
  // Build query string from filters
  // Return fetch promise
}

